<!--
  Health Connect wiring for android/app/src/main/AndroidManifest.xml
  ─────────────────────────────────────────────────────────────────────────────
  Merge these three blocks into the AndroidManifest.xml that `npx cap add android`
  generates. They are shown in-place; do not paste the file verbatim.
-->

<!-- (1) READ permissions — siblings of the existing <uses-permission> lines, ABOVE <application>. -->
<uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
<uses-permission android:name="android.permission.health.READ_SLEEP" />
<uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />

<!-- (2) Package visibility — sibling of <application> (Android 11+ requires this to
         see / talk to the Health Connect provider and its rationale intent). -->
<queries>
    <package android:name="com.google.android.apps.healthdata" />
    <intent>
        <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
    </intent>
</queries>

<!--
  (3) Inside <application>:

  (3a) Add the privacy-policy / rationale intent-filter to the generated
       <activity android:name=".MainActivity" ...> so Android 13- can route the
       "see why this app needs your data" link back to the app:

      <intent-filter>
          <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
      </intent-filter>

  (3b) Add this activity-alias (sibling of MainActivity) for Android 14+ which uses
       the VIEW_PERMISSION_USAGE rationale entry point instead:
-->
<activity-alias
    android:name="ViewPermissionUsageActivity"
    android:exported="true"
    android:targetActivity=".MainActivity"
    android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
    <intent-filter>
        <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
        <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
    </intent-filter>
</activity-alias>
