# Production build config — consumed by `npm run build` (Vite production mode).
# ─────────────────────────────────────────────────────────────────────────────
# These are PUBLISHABLE values only: the project URL and the anon/publishable
# key. They are safe in the client bundle BY DESIGN — Row Level Security is the
# real boundary (see src/lib/supabaseClient.js and .env.example). They are
# committed so the GitHub Actions Android build bakes in real Supabase config;
# without them the bundle ships `createClient(undefined, undefined)`, which
# throws at module load and boots the Capacitor WebView to a black screen.
#
# Render keeps its own dashboard env vars, which take precedence over this file
# on that platform — so this changes nothing for the web deploy, it only feeds
# the CI/native build that had no env at all.
#
# REAL secrets (BBF_ADMIN_TOKEN, coach-agent tokens, service-role keys) are NEVER
# build-time vars — they stay runtime-injected per CLAUDE.md §7. Do not add them here.
VITE_SUPABASE_URL=https://ihclbceghxpuawymlvgi.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImloY2xiY2VnaHhwdWF3eW1sdmdpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzYyOTk1MDIsImV4cCI6MjA5MTg3NTUwMn0.0f7d1aqtygMR__QiyYYUB87yrFLaSRihVQdiFaIhsP0

# Vanguard Blueprint Override — live subscription prices (acct_1TLzQCQ4j3uHTi7P),
# recorded here for reference. NOTE: nothing in the frontend source currently reads
# these — every other tier's priceId is hardcoded directly in
# src/lib/pricingMatrix.js (the actual functional single source of truth, kept in
# lockstep with bbf-create-checkout/PRICE_INFO + stripe-webhook/PRICE_TO_TIER), and
# these two now are too. Adding a real consumer of these env vars is a separate,
# unrequested refactor of that existing convention — not done here.
VITE_STRIPE_PRICE_BLUEPRINT_BASIC=price_1Tv2eWQ4j3uHTi7PZ1XeaN0Q
VITE_STRIPE_PRICE_BLUEPRINT_PRO=price_1Tv2flQ4j3uHTi7PtOF9vEnL
